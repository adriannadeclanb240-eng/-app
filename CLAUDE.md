# 个人账本（Personal Ledger）

> 一款帮助用户记录人民币花销的跨平台桌面记账应用。

## 一、项目基本信息

- 应用名称：个人账本
- 支持平台：Windows、macOS
- 目标用户：个人用户（需要简单、易用的日常记账）
- 货币单位：人民币（元，金额保留 2 位小数）

## 二、核心功能（MVP 第一版）

1. **记一笔支出**：录入金额、选择「一级分类 + 二级分类」、选择日期、填写备注（可选）
2. **支出记录列表**：按日期倒序查看所有历史记录，支持删除某条记录
3. **分类体系**：内置一套默认两级分类，用户可自行增删改（自定义分类）

## 三、两级分类设计（一级大类 → 二级小类）

| 一级大类 | 二级小类 |
|---|---|
| 餐饮 | 早餐、午餐、晚餐、零食、饮料、外卖、聚餐、其他 |
| 交通 | 公交、地铁、打车、加油、停车、火车/飞机、车辆保养、其他 |
| 购物 | 服装、日用品、数码电子、美妆护肤、家居用品、书籍、其他 |
| 居住 | 房租、水电费、燃气费、物业费、维修、装修、其他 |
| 娱乐 | 电影、游戏、旅游、运动健身、KTV/聚会、会员订阅、其他 |
| 医疗健康 | 挂号、药品、体检、治疗、保险、其他 |
| 教育 | 学费、培训课程、书籍资料、文具、考试报名、其他 |
| 人情往来 | 礼物、红包、请客、婚礼/随礼、捐款、其他 |
| 通讯 | 话费、流量、宽带、其他 |
| 其他 | 临时补充 |

## 四、数据存储

- 本地 JSON 文件，数据保存在用户本机的一个 `ledger.json` 文件中（可用记事本直接打开查看）
- 不联网、不上传，隐私安全

## 五、技术栈

- Electron + React + TypeScript
- 构建/打包：electron-vite + electron-builder
- 本地存储：JSON 文件（Node.js 内置文件读写，无需数据库、无需编译）
- 产物：Windows 用 `.exe` 安装包，macOS 用 `.dmg` 安装包

## 六、非功能需求

- 界面为中文
- 数据本地化，隐私安全
- 简单易用，无需注册登录

## 七、工作约定（非常重要，全程遵守）

1. 用户是编程小白，首次使用 Claude Code
2. **任何技术选型/技术方案，必须由 Claude 先列出多个方案，用大白话解释每个方案的优劣势，再由用户拍板决定；Claude 不得擅自替用户做技术决定**
3. 解释技术概念时用通俗语言，避免专业术语
4. 遇到需要用户决策的岔路，用清晰的选项让用户选择

## 八、未来可扩展（本期不做）

- 收入记录（目前只记支出）
- 月度/年度统计报表与图表
- 预算与超支提醒
- 数据导出（Excel/CSV）
- 手机端

## 九、如何运行与开发

- **启动开发版（实时预览）**：在项目根目录打开终端，运行 `npm run dev`，会自动弹出应用窗口
- **打包安装包**：
  - Windows：`npm run build:win`（生成 `.exe` 安装包）
  - Mac：在 Mac 电脑上运行 `npm run build:mac`（生成 `.dmg` 安装包）
- 首次运行前需先执行 `npm install` 安装依赖
